import React from "react";
import { View, Text, StyleSheet } from "react-native";

const AllScreen = ({ item }) => {
  return (
    <View>
      <Text>AllScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default AllScreen;
